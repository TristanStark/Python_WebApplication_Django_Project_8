A TXT file containing the link to the publicly accessible repository on GitHub (or equivalent), containing a Django project with:
A set of instructions detailing the environmental setup and running of the app.
The application, which meets the requirements set out in the Requirements Specification and with a database structure matching that of the database schema.
A UI matching the one designed in the wireframes, rendered server-side using the Django template language.
